# Git Basics

## What is a Commit?
A commit is a recorded snapshot of changes in a repository at a specific point in time.

## What is a Branch?
A branch is a separate timeline used to safely develop features before merging into main.